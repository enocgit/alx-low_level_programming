#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 *
 *
 */

int _isupper(int c)
{
	int answer = isupper(c);
	if (answer > 0)
	{
		printf("%d\n", 1);
	}
	else
		printf("%d\n", 0);

	return (0);
}
