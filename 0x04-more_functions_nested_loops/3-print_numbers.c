#include "main.h"

/**
 * print_number - print numbers to stdout
 *
 * Description: prints numbers to stdout
 */

void print_numbers(void)
{
	int num = 0;
	while (0 <= 9)
	{
		_putchar(num);
		num++;
	}
	_putchar('\n');
}
